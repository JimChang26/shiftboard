# CLAUDE.md

給 Claude Code 的專案說明。修改程式碼前請先讀完「硬性限制」與「資料模型」兩節。

## 專案是什麼

`shiftboard`（排班台）是一個**純前端的自動排班工具**，部署在 GitHub Pages。使用者輸入人員、班別與規則，按一個鍵產生班表，可手動微調、匯出 CSV。

使用者是台灣的小型店家或部門主管，不是工程師。介面全部用**繁體中文**，用詞以現場語彙為準（班別、覆蓋、不可排日期），不要出現 slot、constraint、solver 這類技術詞。

## 硬性限制

這些是專案的前提，不要在沒有明確討論的情況下打破：

- **單一檔案**：所有 HTML、CSS、JavaScript 都在 `index.html` 裡。不要拆成多檔、不要引入 npm 或 build step。
- **無後端、無框架**：原生 DOM API，不要引入 React/Vue/jQuery。目前唯一外部資源是 Google Fonts。
- **使用 localStorage 持久化設定**：每次畫面重新渲染時自動寫入 `localStorage`（key: `shiftboard.state.v1`），重新整理頁面會還原上次的狀態；「下載設定 JSON / 載入設定 JSON」保留作為手動備份與跨裝置分享用途。
- **必須能離線靠 file:// 開啟**（字型會退回系統字型，其餘功能不受影響）。

## 檔案結構

```
index.html    ← 整個應用程式
CLAUDE.md     ← 本檔
```

## 功能規範

1. 排班以月分為單位只需要日班一個班別即可
2. 無論六日都需要排班
3. 可設置每日最低上班人數
4. 人員設定儲存在 localStorage 更新時自動刷新,避免每次需要重新設定(可匯出匯入設定)
5. 可設定每人每月休假天數下限（至少要休多少天）
6. 每人可設定當月的指令休假日期
7. 可設定連續上班上限 
8. 可預先輸入前月已連續上班日避免跨越造成超出連續上班上限
9. 可設定至少連休(連續兩天以上的休假日)次數

## 設計語彙

改動樣式時沿用既有 token，不要另外發明配色：

- `--paper #E7ECEF` 底色、`--ink #15212B` 文字、`--accent #0E6F6C` 主色、`--alert #C63A28` 違規
- 班別顏色由使用者自訂，目前僅單一日班，預設顏色 `#0E6F6C`（沿用主色）
- 字型：標題與數字 Archivo，內文 Noto Sans TC，數據 Roboto Mono
- 班表寬度固定撐滿容器（`table-layout:fixed`），日期欄位依天數平均壓縮，一般情況不需要橫向捲動；人員欄與日期列仍是 sticky，容器仍保留 `overflow-x:auto` 作為極端情況的保險
- 不可排日期用斜線網底，違規格子用紅框，兩者不要換成純色底（會跟班別色打架）

## 品質底線

- 鍵盤可用：班表格子有 `tabindex`，Enter / Space 等同點擊，focus 樣式不要移除
- 使用者輸入一律經過 `esc()` 再塞進 `innerHTML`
- 尊重 `prefers-reduced-motion`

## 部署

推到 `main` 分支根目錄即可，GitHub Pages 直接服務 `index.html`，沒有 build 也沒有 CI。

## 開發時的提醒

- 改完請自行在瀏覽器開啟驗證，特別是：切換月份時的天數重新計算、連續上班上限與最少連續天數的交互、載入舊格式 JSON／localStorage 的相容性。
- 動到 `S` 的結構時要一併考慮「載入設定」讀到舊格式檔案的情況，至少不能整頁壞掉。
- 不要為了功能而增加使用者要填的欄位。這個工具的價值在於按一鍵就有堪用的班表。